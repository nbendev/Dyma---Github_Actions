name: Mon premier workflow
on: [push]
 
jobs:
 say-hello: 
   runs-on: ubuntu-latest
   steps:
     - run: echo ${{ 42 }}
     - run: echo ${{ 'main' == 'top' }}
     - run: echo ${{ contains(github.ref, 'main') }}
